# Python deps for the Vercel serverless function (api/index.py).
# Mirrors backend/requirements.txt (runtime only) and is intentionally slim so
# the bundle fits Vercel's serverless size limit (no torch / chromadb / pandas).
fastapi==0.115.6
python-multipart==0.0.20
pydantic==2.10.4
pydantic-settings==2.7.1
PyJWT==2.10.1
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
pgvector==0.3.6
supabase==2.11.0
google-generativeai==0.8.3
PyMuPDF==1.25.1
python-docx==1.1.2
openpyxl==3.1.5
tenacity==9.0.0
httpx==0.27.2

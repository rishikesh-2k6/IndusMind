# ---- Web framework ----
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
pydantic==2.10.4
pydantic-settings==2.7.1

# ---- Auth ----
PyJWT==2.10.1

# ---- Databases (Supabase Postgres + pgvector) ----
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
pgvector==0.3.6
supabase==2.11.0

# ---- LLM + embeddings (Gemini only; keeps the serverless bundle small) ----
google-generativeai==0.8.3

# ---- Document parsing ----
PyMuPDF==1.25.1
python-docx==1.1.2
openpyxl==3.1.5

# ---- Utilities ----
tenacity==9.0.0
httpx==0.27.2

# ---- Optional OCR (heavy; NOT for Vercel. Enable locally with ENABLE_OCR=true) ----
# paddleocr==2.9.1
# paddlepaddle==2.6.2
# Pillow==11.0.0

# ---- Tests ----
pytest==8.3.4
pytest-asyncio==0.25.0
